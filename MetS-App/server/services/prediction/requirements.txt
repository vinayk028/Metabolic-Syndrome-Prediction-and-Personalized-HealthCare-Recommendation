# Requirements for Python Prediction Service (Flask API)
# This service performs Bayesian Network inference for MetS prediction

# Web Framework
flask>=2.0.0
flask-cors>=3.0.0

# Bayesian Network Inference
pgmpy>=0.1.20
numpy>=1.21.0

# Optional: For better performance
pandas>=1.3.0

# Note: The model (bayesian_network_model.pkl) is pre-trained
# For training the model, use the root requirements.txt which includes:
# - deap (Genetic Algorithm)
# - scikit-learn (ML utilities)
# - keras (Deep learning, if needed)
# - jupyter (For training notebooks)